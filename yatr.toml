# Generated by yatr rollout — adjust as needed.

[tasks.dev]
run = ["npm run dev"]

[tasks.build]
run = ["npm run build"]

[tasks.start]
run = ["npm run start"]

[tasks.ci]
desc = "CI pipeline"
depends = ["build"]
